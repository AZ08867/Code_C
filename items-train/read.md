# items-train — 简易物品管理示例

这是一个用 C 编写的简易物品库存示例，演示了结构体、数组和基本的增删改查操作。

## 快速开始

```sh
clang *.c -o items.exe
.\items.exe
```

程序会在控制台输出初始库存并演示更新操作。

## 主要文件

- `main.c`：示例主程序，包含 `Item` 结构体、添加与更新函数，以及打印库存的示例。
